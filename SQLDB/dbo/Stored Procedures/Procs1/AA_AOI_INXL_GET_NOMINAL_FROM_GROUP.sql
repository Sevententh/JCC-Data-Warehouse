CREATE PROCEDURE [dbo].[AA_AOI_INXL_GET_NOMINAL_FROM_GROUP]
@GUID VARCHAR (36) NULL, @tyear VARCHAR (1) NULL, @period INT NULL, @get_count_only TINYINT NULL, @include_concat_analysis BIT NULL, @rows BIGINT NULL OUTPUT
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


