CREATE PROCEDURE [dbo].[AA_AOI_INXL_RECORDLOOKUP_EXEC]
@dt_code VARCHAR (6) NULL, @table VARCHAR (1000) NULL, @code_field VARCHAR (25) NULL, @name_field VARCHAR (50) NULL, @where VARCHAR (1000) NULL
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


