CREATE PROCEDURE [dbo].[AA_FORMAT_ADDRESS_S]
@Address VARCHAR (160) NULL, @Postcode VARCHAR (12) NULL, @Town VARCHAR (30) NULL, @County VARCHAR (30) NULL, @Name VARCHAR (40) NULL, @Contact VARCHAR (30) NULL, @Country VARCHAR (35) NULL, @Report_Format BIT NULL, @ReturnValue VARCHAR (512) NULL OUTPUT
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


