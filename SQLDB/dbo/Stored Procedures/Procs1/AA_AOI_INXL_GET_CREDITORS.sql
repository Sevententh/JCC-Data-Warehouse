CREATE PROCEDURE [dbo].[AA_AOI_INXL_GET_CREDITORS]
@dt_code VARCHAR (6) NULL, @year VARCHAR (1) NULL, @period INT NULL, @get_count_only TINYINT NULL, @rows BIGINT NULL OUTPUT
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN
END


