CREATE FUNCTION [dbo].[AA_BIT_SET_F]
(@PS_Value BIGINT NULL, @PS_BitPos BIGINT NULL, @PS_OnOff BIT NULL)
RETURNS BIGINT
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

