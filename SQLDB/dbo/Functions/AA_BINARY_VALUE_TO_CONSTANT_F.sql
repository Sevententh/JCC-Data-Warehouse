CREATE FUNCTION [dbo].[AA_BINARY_VALUE_TO_CONSTANT_F]
(@PS_BinaryValue VARBINARY (MAX) NULL)
RETURNS VARCHAR (MAX)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

