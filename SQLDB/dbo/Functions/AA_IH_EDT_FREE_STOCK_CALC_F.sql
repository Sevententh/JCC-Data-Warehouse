CREATE FUNCTION [dbo].[AA_IH_EDT_FREE_STOCK_CALC_F]
(@Physical FLOAT (53) NULL, @Allocated FLOAT (53) NULL, @UnAllocated FLOAT (53) NULL, @OnOrderIn FLOAT (53) NULL, @QtyPrinted FLOAT (53) NULL)
RETURNS FLOAT (53)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

