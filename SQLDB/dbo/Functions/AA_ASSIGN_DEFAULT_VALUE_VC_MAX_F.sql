CREATE FUNCTION [dbo].[AA_ASSIGN_DEFAULT_VALUE_VC_MAX_F]
(@PS_DefaultString NVARCHAR (255) NULL, @PS_Mode NVARCHAR (1) NULL, @PS_DefaultStringCharPos TINYINT NULL, @PS_SuppliedValue VARCHAR (MAX) NULL, @PS_DefaultValue VARCHAR (MAX) NULL, @PS_ExistingValue VARCHAR (MAX) NULL, @PS_InvalidValue VARCHAR (MAX) NULL)
RETURNS VARCHAR (MAX)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

