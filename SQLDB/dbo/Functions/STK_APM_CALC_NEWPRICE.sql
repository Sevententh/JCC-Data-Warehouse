CREATE FUNCTION [dbo].[STK_APM_CALC_NEWPRICE]
(@baseprice FLOAT (53) NULL, @sthtype VARCHAR (10) NULL, @stptype TINYINT NULL, @stpfixed FLOAT (53) NULL, @stpdisc FLOAT (53) NULL, @stpdisccash FLOAT (53) NULL, @stpbogoftype TINYINT NULL, @stpchain FLOAT (53) NULL)
RETURNS FLOAT (53)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

