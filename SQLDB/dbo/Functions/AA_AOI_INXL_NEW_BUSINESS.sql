CREATE FUNCTION [dbo].[AA_AOI_INXL_NEW_BUSINESS]
(@customer VARCHAR (25) NULL, @year VARCHAR (1) NULL, @project VARCHAR (10) NULL)
RETURNS VARCHAR (1)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

