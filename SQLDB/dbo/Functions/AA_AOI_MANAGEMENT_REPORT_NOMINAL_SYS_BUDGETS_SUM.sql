CREATE FUNCTION [dbo].[AA_AOI_MANAGEMENT_REPORT_NOMINAL_SYS_BUDGETS_SUM]
(@ncode VARCHAR (25) NULL, @year VARCHAR (1) NULL, @period INT NULL, @ytd INT NULL, @revision INT NULL)
RETURNS FLOAT (53)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

